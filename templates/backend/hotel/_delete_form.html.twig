<form method="post" action="{{ path('backend_hotel_delete', {'id': hotel.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet établissement ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hotel.id) }}">
    <button class="btn btn-secondary">Supprimer</button>
</form>
